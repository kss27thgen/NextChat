import React from "react";

const LoadingSpinner = () => {
	return (
		<>
			<p className="loadingSpinner">
				<img src="/loading-trans.jpeg" alt="Loading..." />
			</p>
		</>
	);
};

export default LoadingSpinner;
